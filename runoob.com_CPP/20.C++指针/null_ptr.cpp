#include <iostream>
using namespace std;

int main()
{
    int *ptr = NULL;
    cout << "ptr的值是：" << ptr << endl;
    /*
     * if(ptr) 如果ptr非空，则完成
     * if(!ptr) 如果ptr为空，则完成
     */

    return 0;
}
